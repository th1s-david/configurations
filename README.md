# configurations

ok hi guys to install run:
```bash
git clone https://github.com/th1s-david/configurations/
cd configurations
bash install.sh
```
you need bash to install ts ts ts ts ts unless you manually move the files into the correct directories
